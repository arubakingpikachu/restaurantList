<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Document</title>
  <link rel="stylesheet" href="/stylesheets/mystyle.css">
  <link rel="stylesheet" href="/stylesheets/bootstrap.css">
</head>
<header>
  <!-- navigation -->
  <nav class="navbar navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="/">
          <img src="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png" width="30" height="30" class="d-inline-block align-top" alt="好。吃">
          {{#if isAuthenticated}}{{ user.name}}'s {{/if}}的餐廳清單
      </a>
      {{#if isAuthenticated}}
      <a class="btn btn-primary text-align" href="./users/logout"class="text-primary">Logout</a>
      {{else}}
      <a class="btn btn-primary text-align" href="./users/login"class="text-primary">Login</a>
      {{/if}}
    </div>
  </nav>

  <div class="banner"></div>
</header>
<body>
  
  <script src="/javascripts/popper.js"></script>
  <script src="/javascripts/bootstrap.js"></script>
  
  {{{ body }}}
</body>
</html>